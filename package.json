{
  "name": "shuizhu-portfolio",
  "version": "1.0.0",
  "description": "水煮｜后期合成全体系课程 - 专业作品集网站",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cross-env NODE_ENV=production node scripts/gh-pages-build.mjs",
    "deploy": "npm run build && node -e \"require('fs').writeFileSync('out/.nojekyll','')\"",
    "git:init": "git-init.bat",
    "git:config": "echo '请运行：' && echo 'git config --global user.name \"你的GitHub用户名\"' && echo 'git config --global user.email \"你的GitHub邮箱\"'"
  },
  "keywords": [
    "portfolio",
    "photography",
    "photoshop",
    "course",
    "chinese"
  ],
  "author": "水煮老师",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
